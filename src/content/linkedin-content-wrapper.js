// LinkedIn Content Script Wrapper - No exports to avoid module issues
// This file imports the main content script but doesn't export anything
// This ensures the bundled output has no ES6 module exports

import './linkedin-content.js';

// No exports - this ensures the bundled content script works as a traditional script